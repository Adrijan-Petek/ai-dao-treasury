{
  "name": "ai-dao-treasury",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network hardhat"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.2.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  }
}